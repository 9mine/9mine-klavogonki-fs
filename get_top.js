const puppeteer = require('puppeteer'); (async () => { try { const browser = await puppeteer.launch({ args: ['--lang=ru-RU,ru'] }); const page = await browser.newPage(); await page.goto("http://klavogonki.ru"); await page.waitForSelector('.ng-binding', 0); const body = await page.evaluate(() => { return document.getElementById("top_today").innerHTML; }); process.stdout.write(body); await browser.close(); } catch (error) { console.log(error); }; })();
